# 项目概览

## 项目定位

`<project-name>` 是一个 `<project-type>` 项目。

## 当前区域划分

根据项目实际保留需要的区域：

- `frontend`
- `backend`
- `shared`
- `contract`
- `data`
- `ops`

## 建议起步顺序

1. 先读 `AGENTS.md`
2. 再读 `project-memory/SYSTEM/`
3. 再读 `project-memory/docs/`，优先看 `FAQ.md`、`LOCATE.md`、`CHANGE_GUIDE.md`
4. 倒推复杂链路、事件绑定、request、state 流转时查询 `project-memory/kb/`
5. 只有 docs 与 KB 都不足时，才做大范围仓库搜索
