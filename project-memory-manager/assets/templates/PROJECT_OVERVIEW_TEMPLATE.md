# 项目概览

## 项目定位

`<project-name>` 是一个 `<project-type>` 项目。

## 当前区域划分

根据项目实际保留需要的区域：

- `frontend`
- `backend`
- `shared`
- `contract`
- `data`
- `ops`

## 建议起步顺序

1. 先读 `AGENTS.md`
2. 再读 `project-memory/SYSTEM/`
3. 再读 `project-memory/docs/`
4. 只有倒推复杂链路时才查询 `project-memory/kb/`
